#ifndef SORT_H
#define SORT_H

#include <stdio.h>
#include <stdlib.h>
/* printing */
void print_array(const int *array, size_t size);

/* sorting algo */
void bubble_sort(int *array, size_t size);

#endif /* sort.h */